# EdgeCase Equalizer — development/test requirements.
# Matches the [dev] extra in pyproject.toml.
#
# Usage: pip install -r requirements-dev.txt

-r requirements.txt
pytest==9.0.2
ruff==0.15.18      # linter / static analysis — see [tool.ruff] in pyproject.toml
