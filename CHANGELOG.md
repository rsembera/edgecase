# EdgeCase Equalizer - Changelog

All notable changes after the initial v1.0 release (March 2026) are documented here.

Format: Each entry includes date, version (if applicable), and description.

---

## [Unreleased]

### 2026-06-14 (crypto v2 — password change, Stage 5)
- **The master-password change is now v2-aware.** On a migrated (v2) install, `change_password_progress` runs `migrate_crypto.change_password()`: it derives the old keys from the current `.keyinfo`, derives fresh keys from the new password under a new salt, takes a full backup, re-encrypts every file from the old file key to the new one (atomic per file), rebuilds the database under the new raw key as a separate verified file, and commits by swapping it in and writing the new `.keyinfo`. It reuses the migration's crash-safe machinery: a `.v2_migrating` marker (kind `rekey_v2`) covers the whole operation, and `recover_if_interrupted()` gained one additive branch — for a `rekey_v2` marker it finalizes vs. rolls back by comparing the on-disk `.keyinfo` salt to the marker's recorded new salt (still password-free; the v1→v2 recovery logic is unchanged). A crashed password change therefore always lands on cleanly-old-password or cleanly-new-password, never locked out. The route closes the live DB handle, runs the rekey, and forces a fresh login; on failure the runner has already rolled back. v1 installs keep the existing Fernet/passphrase flow untouched. 6 new tests (happy path, rollback, both recovery branches; the rollback test confirms the old `.keyinfo` is restored). Full suite: 116 passed.

### 2026-06-14 (crypto v2 — login-flow wiring, Stage 4b)
- **The v1→v2 migration now runs automatically at login.** `recover_if_interrupted()` is called at the top of `login()` (before any DB open; a password-free no-op when nothing is pending). After a successful password check on an existing v1 install, `login()` closes the DB handle, hands the password to a single-use server-side token (never the cookie — mirroring the password-change handoff), and renders an `upgrading.html` interstitial. That page opens an SSE connection to the new `/auth/migrate/stream` route, which runs `migrate_crypto.migrate()` (full backup → re-encrypt → rekey → commit, all crash-safe) then opens the now-v2 database, stores it, calls `init_all_blueprints`, and completes the login exactly as the normal path does. The stream route is in `require_login`'s allowed endpoints (it runs while `config['db']` is intentionally unset, gated by the token). On failure the runner has already rolled back to v1 and the page says so. First-run installs are created on v1 as before and migrate on their next login. 5 wiring tests (handoff single-use/expiry/bad-token; stream reachable-without-DB + tokenless error; login still renders). Full suite: 110 passed.
- **Caveat:** the change-password flow is not yet v2-aware (Stage 5). It assumes Fernet files + passphrase rekey, so it must not be used on a migrated install until Stage 5 lands.

### 2026-06-14 (crypto v2 — migration runner, Stage 4a)
- **`core/migrate_crypto.py`: the in-place v1→v2 migration runner**, with a crash-safe state machine — built and tested, but imported by no production path yet (wiring into the login flow is Stage 4b). Sequence: full verified backup (reuses `utils.backup`) → write a `.v2_migrating` marker → re-encrypt every v1 file in place (atomic per file, resumable via content-sniff) → build and verify the raw-keyed DB as a *separate* file (`integrity_check` + row-count parity, original untouched until it passes) → commit (atomic DB swap, clear stale WAL, then write `.keyinfo` as the commit point) → drop the marker. `recover_if_interrupted()` runs at startup needing no password: `.keyinfo` is written only after the DB swap, so its presence proves the migration reached commit (→ finalize), and its absence with a marker present means an interrupted run (→ roll back to the backup). No crash window can leave the user locked out — the result is always fully-v2 or cleanly-v1. The runner only touches files the backup also covers (all attachments + assets logo/signature), so rollback can always restore anything it changed. 5 tests: happy path, immediate rollback on failure, recovery-finalize, recovery-rollback, and detection; fast-KDF fixture keeps them quick. Full suite: 105 passed.

### 2026-06-14 (crypto v2 — dry-run harness + raw-key DB keying)
- **Dry-run migration harness** (`tools/migrate_crypto_dryrun.py`): copies a data root to a scratch directory and runs the entire v1→v2 migration there — re-encrypt every file, `sqlcipher_export` the database to a raw v2 key, and verify round-trips, integrity, row-count parity, and the key-info token — touching nothing live. Password read via getpass (never an argument). Validated on real data: 39 files migrated, 364 rows preserved across 13 tables, integrity_check ok, Argon2id derive 0.74s, PASS.
- **`database.py` now keys SQLCipher with the raw v2 key when a `.keyinfo` file is present.** It derives the key (Argon2id → HKDF, cached per password so the ~0.74s derive runs once, not per connection) and opens with `PRAGMA key = "x'…'"`. With no `.keyinfo`, the passphrase path runs exactly as before, so un-migrated (v1) installs are wholly unaffected — verified by the full suite still passing. `verify_password` checks the key-info verification token for v2 installs (no database open needed). New test opens a real raw-keyed database through `Database` and confirms the passphrase no longer opens it. Full suite: 100 passed.

### 2026-06-14 (attachment crypto v2 — primitives)
- **New v2 encryption module (`core/encryption_v2.py`), not yet wired into any production path.** First step of migrating attachment/asset/statement-PDF encryption from Fernet (PBKDF2-SHA256 / AES-128) to an Argon2id → HKDF → AES-256-GCM scheme matching MailRepo's design — using `argon2-cffi` for the Argon2id KDF (matching MailRepo; `cryptography`'s own Argon2id measured ~5× slower on the M4 — ~3.9s vs ~0.74s for identical params), with `cryptography` retained for HKDF/AES-GCM. Argon2id (256 MiB, t=6, p=1) derives a 32-byte master key; HKDF-Expand with domain-separated labels splits it into a raw SQLCipher DB key (for `PRAGMA key = "x'…'"`) and a file key. Files use wire format `[0x02][12-byte nonce][ciphertext+tag]` with the version byte bound into the GCM AAD. v1 Fernet tokens (urlsafe-base64, leading `g` / `0x67` on disk) and v2 blobs (`0x02` prefix) are unambiguously distinguishable, so both stay readable during migration. A versioned key-info file (`.keyinfo`: magic + Argon2id salt + verification token) is written alongside the existing `.salt`, which is left untouched so v1 files remain decryptable through the bake period. 16 unit tests added (round-trip across sizes incl. empty, version dispatch, wrong-key and tamper rejection, KDF determinism and salt-sensitivity, subkey domain separation, key-info round-trip and bad-magic). Full suite: 99 passed.

### 2026-06-12 (AI Scribe change review)
- **"Show Changes" on the AI Scribe screen.** After the Scribe generates (or whenever the result pane has content), a Show Changes toggle swaps the result pane for a read-only overlay marking exactly what changed against the original notes — deletions struck in red, insertions highlighted in green, word-level, including punctuation-only edits. Toggle back to keep editing; hand-edits invalidate the diff and it recomputes on next view; Keep Changes stays available while reviewing. The diff is computed server-side by a new full-text variant (`generate_full_content_diff`) of the same word-level engine the amendment history uses — full text, no truncation or context elision — served by `POST /api/ai/diff`, with note content HTML-escaped before markup per M17. Real-time diffing during streaming was considered and rejected: a diff against a partial stream marks all not-yet-generated text as deleted, which is noise.

### 2026-06-11 (silent validation failures made visible)
- **A blocked save now says why.** The session form's required selects are hidden by Choices.js; when one is invalid, the browser refuses to submit but cannot focus the hidden control to show its validation bubble — verified in headless Chromium to produce no bubble and no console output. The save button simply appeared dead. Submit-button clicks are now intercepted: an invalid form gets native validation bubbles when every invalid field is visible, or an in-app "Missing Required Information" modal naming the fields when any is hidden. Investigated after a report of "Save Changes needs two clicks" on a locked session — that exact symptom could not be reproduced with synthetic data (first click saves correctly in both jsdom and real Chromium on a valid form), so whatever field is invalid on the affected page will now identify itself instead of failing silently.

### 2026-06-11 (tab-close warning restored)
- **Native unsaved-changes warning on tab close/reload is back.** Deferred from 2026-06-10 once its three misfire causes were identified and fixed (form bound by id; baseline re-sync after async picker init; capture-phase guard preempting the disconnect handler). The guard prompts only when the form is genuinely dirty, and is disarmed on an un-prevented form submit and on the modal's Leave Without Saving — so it never stacks with the modal and never fires on a deliberate leave. Verified in the jsdom harness (real rendered page including base.html's inline scripts) across six scenarios: natural picker re-baseline, no false-dirty on untouched forms, prompt on dirty unload, modal-without-navigation on dirty in-app clicks, persistence after Stay Here, and silence after Leave.

### 2026-06-10 (locked-entry audit coherence)
- **Unsaved-changes protection on the session form (all modes)**: there was previously no guard at all — navigating away from a half-written session note via Cancel or Prev/Next lost it silently. Dirty state (snapshot comparison, see below) now gates in-app navigation with a custom Unsaved Changes modal (Stay Here / Leave Without Saving). Root cause of a long misfire chase: base.html's server-disconnect handler intercepts every same-origin link click in the document capture phase and navigates programmatically, so a bubble-phase guard could open the modal but never stop the navigation — the (interim) beforeunload warning was the only thing halting the page, surfacing as a native dialog stacked on the modal in every browser. The guard now runs in the window capture phase and stops propagation when dirty, so the disconnect handler never starts; clean clicks keep the normal liveness-checked navigation. Verified in a jsdom harness running the real rendered page including base.html's inline scripts. No tab-close/reload warning is registered (deliberate; the modal covers the in-app loss paths).
- **Fix: dirty detection bound the wrong form on edit pages.** The first cut selected `form[method="post"]`, but edit-mode pages render a second POST form first (the redact button component; HTML matches `method` case-insensitively), so dirty detection silently never fired on saved/locked sessions — exactly the pages that need it most. The entry form now has `id="session-entry-form"` and is selected by id.
- **Cancel relabelled "Back" on locked entries**: when reviewing a locked session there's nothing to cancel; the label now reflects that. New entries and drafts keep "Cancel".
- **No-change saves of locked sessions are now true no-ops**: clicking "Save Changes" on a locked session with no edits previously wrote nothing to the amendment history (already correctly guarded) but still bumped `modified_at` — leaving the record asserting a modification the amendment trail doesn't show. The route now returns without writing when the change set is empty.
- **"Save Changes" on locked sessions is disabled until the form is edited**: the button reads "No Changes" and activates when the form state actually differs from what was loaded (snapshot comparison in `session.js`, since the date/time pickers write values without firing events; restoring a field to its original value re-disables it). UI polish on top of the backend guard, not a substitute for it.

### 2026-06-09 (request-layer security)
- **Per-client session authentication enforced**: `require_login` previously gated only on the app-global unlocked database — `session['authenticated']` was set at login but never checked, so any cookieless request (another device in LAN mode, or any local process) had full access the moment anyone was logged in anywhere. The session marker is now required; each browser/device must present the master password to get its own session. Login already re-verifies the password by opening the database with it, so no change was needed there.
- **Host-header validation (DNS rebinding protection)**: localhost binding alone doesn't stop a malicious web page from rebinding its domain to 127.0.0.1 and making what the browser then treats as same-origin requests — which also bypassed the CSRF JSON exemption's safety rationale. A new first-in-line `before_request` rejects (403) any request whose Host isn't localhost/127.0.0.1/::1; LAN mode (`EDGECASE_LAN=1`) additionally admits private-range and Tailscale CGNAT (100.64/10) IP literals but never DNS names; `EDGECASE_ALLOWED_HOSTS` (comma-separated) covers e.g. Tailscale MagicDNS hostnames. Applies to every endpoint including login and static files.
- **LAN mode plaintext warning**: starting with `--lan` now prints a prominent warning that LAN mode serves unencrypted HTTP (master password and PHI cross the network in plaintext) and recommends Tailscale/SSH tunneling over localhost instead. `--lan` also now sets `EDGECASE_LAN=1` so the Host-validation layer sees one canonical signal.
- **AI model download integrity**: pinned the official SHA-256 of the Hermes 3 Q4_K_M GGUF (verified independently against HuggingFace's LFS metadata and a local copy). Both download paths verify before accepting the file — the blueprint's streaming download hashes incrementally during the stream (no second pass over ~5GB), and `download_model` hashes after `hf_hub_download`. Mismatches are deleted with a clear error.
- **`scribe_save` hardened against malformed JSON**: a request body of JSON `null` (or non-object) previously crashed with an AttributeError → 500; now returns a clean 400.
- **First request-layer tests**: 7 new tests exercise the Flask app via the test client (rebound-Host rejection incl. on the login page, localhost allowance, LAN-mode host rules, cookieless requests rejected while the DB is unlocked, JSON 401 for cookieless API calls, authenticated session passes). 77 tests total.

### 2026-06-07 (fifth batch — distribution support)
- **Startup migrations for distributed installs**: EdgeCase ships as source/.deb/.dmg, but the M2 foreign-key flip and H5 typed-column cleanup were validated only against this machine's database. Now: (1) FK enforcement is **conditional** — `PRAGMA foreign_key_check` runs once at startup; clean databases get enforcement, legacy databases with pre-existing orphans keep working without it plus a warning pointing at `tools/audit_orphans.py` (enforcement enables automatically at the next launch once clean). (2) Legacy `''` values in typed entry columns are **rewritten to NULL automatically at startup** (idempotent) — every install gets the cleanup, not just where `audit_typed_columns.py --fix` was run by hand. Two new tests cover orphaned-legacy-DB and empty-string-legacy-DB startup paths (70 total). Also confirmed: packaged installs (.dmg → `~/Library/Application Support/EdgeCase/`, .deb → `/opt` detection → `~/.local/share/EdgeCase/`) already use stable per-user data roots, so L1 needs no change for distribution; and `is_system_locked` has seeded Inactive since pre-1.0, so M10's flag-based retention sweep is safe on all installs. **Note: the published .deb/.dmg artifacts must be rebuilt to carry any of the June 2026 fixes.**

### 2026-06-07 (fourth batch)
- **AI Scribe lock enforcement (closes the M11 follow-up)**: AI Scribe is by design only available for unlocked drafts (the session form hides the button once locked), but the server routes never checked — a stale tab or direct URL could silently modify a locked clinical record with no edit history. `scribe_page` and `scribe_save` now refuse locked entries (403), and `scribe_save` no longer passes `allow_locked=True`, so the M11 DB-layer guard backs the route check. Locked-entry edits must go through the session edit form, which logs to edit history.
- **Backup→restore round-trip tests** (the review's "Testing assessment" recommendation): two end-to-end tests against an isolated temp data root exercise the real backup module — full backup → mutate DB + attachments → restore (asserts original data back, post-backup changes rolled back, stale WAL/SHM sidecars from H1 not carried over), and a full→incremental chain restore (asserts the incremental state restores exactly, including a file deletion that must not resurrect). Backup tests no longer count zero; 68 tests total.

### 2026-06-07 (third batch)
- **M2 complete — foreign key enforcement enabled**: `tools/audit_orphans.py` run against the production database came back clean (zero orphans across all 10 declared FK relationships; `PRAGMA foreign_key_check` clean), so `PRAGMA foreign_keys = ON` is now set on every connection. All delete paths verified child-first (ledger entry deletes, link groups, client archive/delete). Three new tests assert enforcement is live (orphan inserts and parent-deletes-with-children rejected); 66 tests total. Note: `tools/audit_typed_columns.py` found 9 legacy empty-string values in non-FK typed columns (pre-H5 data) — run it with `--fix` to rewrite them to NULL; they don't affect FK enforcement.

### 2026-06-07 (continued)
- **H3 — Single-guardian billing clarified**: For a minor with only one guardian, `guardian1_pays_percent` was computed but ignored (statements billed the full total). Hid/disabled the "Pays (%)" field in the profile form when there's no second guardian — the percent is only meaningful when there's a G2 to share the bill. Billing logic now matches the displayed intent.
- **H5 — Stop coercing `None` → `''` in entry writes**: `add_entry` previously stored empty strings in INTEGER/REAL columns, breaking range filters, ORDER BY, and `IS NULL` checks (notably the redaction lock's `if statement_id is not None`). Now preserves `None`, and additionally coerces `''` → `None` for the new `TYPED_ENTRY_COLUMNS` set when stray empty strings come in from form posts. Same coercion applied to `update_entry`. Added `tools/audit_typed_columns.py` to find/optionally fix any pre-existing `''` values in typed columns on the real DB.
- **M11 — DB-layer lock enforcement**: `update_entry` now raises `EntryLockedError` if the target entry is locked unless the caller passes `allow_locked=True`. All existing callers updated: user-edit routes already check the lock and log to edit history (now opt in explicitly); `renumber_sessions` opts in as a system invariant; AI Scribe save opts in (edit history for AI Scribe is a known follow-up). Future callers can no longer silently update locked clinical entries.

### 2026-06-07 (later)
- **Ledger reports — silent receipt-dropping fix**: `pdf/ledger_report.py` was resolving `att['filepath']` against the process working directory rather than DATA_ROOT (same root cause as C1/C2). In dev mode CWD happened to equal DATA_ROOT so the bug never surfaced; in desktop/installed mode receipts were silently skipped. Promoted `resolve_attachment_path()` to `core/config.py` as a single source of truth, imported by both `client_export.py` and `ledger_report.py`.

### 2026-06-07 (second batch)
- **Code review remediation — completion pass** (everything remaining in CODE_REVIEW.md except the M2 PRAGMA flip, pending the orphan-audit results, and three documented deferrals):
  - **M1 — Decimal money arithmetic** (with **L11** refund tax reversal and **M9** empty-IN guard): new `core/money.py` + `core/billing.py`; all monetary math is Decimal quantized to cents, storage stays REAL but only receives cent-exact values, comparisons use integer cents. The `<= 0.01` paid-status fudge is gone (99.99 against 100.00 is now correctly *partial*). Guardian percentage splits are per line item with the exact remainder to guardian 2, so statement PDFs sum exactly to portion amounts. Refunds reverse tax pro-rata instead of recording 0. 20 new tests exercise the real billing functions (odd-cent splits, explicit per-item amounts, exact-cent payment status, ten-dimes-pay-a-dollar, refund reversal). Decision documented in Architecture_Decisions.md.
  - **H2** — master password no longer round-trips through the session cookie during password change; server-side single-use token (5-min TTL) instead.
  - **H6/H8** — client deletion removes files only after the transaction commits and cleans up statement_portions/client_links/entry_links; multi-statement link-group writes roll back on error.
  - **H4/H7** — backup chains with a missing incremental are flagged broken and refuse restore; backups checkpoint the WAL themselves, integrity-check the zipped encrypted DB when a db handle is available, and include a leftover WAL file; pre-restore safety backups no longer hide each other (L2); legacy manifest entries resolve to the default backup dir (L3).
  - **M4** — CSRF now required for multipart/form-data (all senders already carried tokens); **M6** — entry edit routes verify client ownership; **M8** — login lockout keys on remote_addr, not spoofable X-Forwarded-For; **M16** — dev-server connection-leak teardown; **M17** — edit-history diffs HTML-escape user content; **L12** — friendly validation instead of 500s on empty names/malformed dates.
  - **M7** — decrypted statement/export PDFs now live in private 0700 temp dirs with randomized names and cleanup (desktop viewer dir cleaned at each launch); **M18** — desktop wrapper picks a free port, detects an already-running instance, and polls for server readiness; **M15** — AI generation holds the model lock for the whole stream; **L4** — per-password Fernet cache (~150× faster file ops after first).
  - **M13** — shared helpers for link-group fees, get-or-create payee/category, and currency formatting (PDF output byte-identical); **L15** — requirements.txt reconciled with pyproject.toml (+ requirements-dev.txt); **L16** — dead code removed; **L17** — only images/PDF/plain-text attachments render inline, everything else downloads; **L5/L14/L18, M10, L6-L9** — N+1 queries, LIKE escaping, verify_password leak, retention-type rename guard, double-submit guards, category-name escaping, picker listener leak, retention modal fix.
  - **Deferred with documented rationale** (Architecture_Decisions.md): L1 (data-root detection — the live install keeps data in the app folder; changing detection risks orphaning it), L13 (naive local-time timestamps — migration risk outweighs DST edge), M5 (home-page N+1 — performance-only, revisit with measurements). **M2 PRAGMA flip** remains pending the orphan audit against the real database.

### 2026-06-07
- **Code review remediation batch** (first pass from CODE_REVIEW.md; one commit per item):
  - **C1+C2 — PDF export attachments fixed**: Client-file exports were silently dropping every attachment — paths were rebuilt from the original filename instead of the stored UUID `.enc` path, and images were embedded without decryption. Exports now resolve `attachments.filepath` against the data root and decrypt before embedding; merged PDF attachments show their original filename on the header page.
  - **H1 — Restore safety**: `complete_restore()` now removes stale `edgecase.db-wal`/`-shm` sidecar files before copying the restored database, preventing SQLite from replaying old WAL frames into the restored file after a crash.
  - **M2 — FK orphan audit (audit only)**: Added `tools/audit_orphans.py`, a read-only script reporting rows that violate the schema's declared (but unenforced) FOREIGN KEYs, plus a `PRAGMA foreign_key_check` cross-check. The `foreign_keys=ON` flip is deliberately deferred pending audit results (see TODO above).
  - **M3 — Indexes**: Added `entries(client_id, class)`, `entries(ledger_type, ledger_date)`, `attachments(entry_id)`, and `statement_portions(client_id, status)` via `CREATE INDEX IF NOT EXISTS` in schema init — existing databases migrate automatically at startup.
  - **M12 — Atomic writes**: `encrypt_file()` and `save_manifest()` now write to a temp file and `os.replace()` instead of truncating in place; a crash mid-write can no longer destroy an attachment or corrupt the backup manifest.
  - **M14 — PDF crash on special characters fixed**: User-entered text (names, addresses, descriptions, payment instructions, attestation, attachment descriptions) is now XML-escaped before interpolation into ReportLab Paragraph markup. Previously a single `&`, `<`, or `>` in any field crashed statement/export/report generation.
  - **L10 — Shared JS utilities**: New `web/static/js/shared_utils.js` (loaded in `base.html`) consolidates `escapeHtml` (4 copies), textarea auto-resize (6 copies), the three-way fee calculation (6 copies), and the color palette; deleted dead `color_palette.js`. Page-level function names kept as thin delegates, so no template/handler changes.
  - All 43 automated tests pass; PDF fixes verified end-to-end against a test database (encrypted image + PDF attachments render in exports; statements/exports/reports generate with `&`, `<`, `>` in every user field).

### 2026-05-16
- **AI Scribe**: Refined Cancel button presentation. Removed the inline "Generating..." status indicator (which was causing column-width reflow) and replaced it with a dedicated Cancel button styled to match the action buttons, placed below them in the same column. Uses `visibility: hidden` to reserve layout space so action buttons no longer jump when Cancel appears or disappears.
- **AI Scribe**: Converted the "Loading AI model..." banner from an inline element to a centered modal overlay with dimmed backdrop. Previously the banner pushed page content down when shown and let it snap back when hidden; the overlay is now `position: fixed` so it never affects layout. The overlay also better reflects the actual user state — the page is unusable until the model loads.

### 2026-05-15
- **AI Scribe**: Added Cancel button to abort in-flight generation. Uses `AbortController` to terminate the SSE stream cleanly, releasing the model so a new action can be started immediately. Previously, hitting the bottom "Cancel" link navigated away but left the generation running on the backend, causing subsequent requests to hang waiting for the model lock.
- **AI Scribe**: Fixed pre-existing bug where the "Generating..." status indicator never appeared. The JS was using `classList.remove('hidden')` to show the status div, but the div was hidden via inline `style="display: none;"` and no `.hidden` CSS class exists. Switched to direct inline style manipulation.
- **AI Scribe**: Renamed bottom "Cancel" link to "Back" with standard styling (left-arrow icon, plain `.btn` class) to match the Back button convention used throughout the rest of the app.

### 2026-04-10
- **Security**: Added `Cache-Control: no-store` headers to attachment viewing and download endpoints. Prevents browsers from caching decrypted attachment content to disk. Decrypted attachments now exist only in memory during viewing/download.

### 2026-04-01
- **Scheduler**: Fixed natural language date parsing to prioritize explicit dates over day-of-week names. Previously, "Thursday April 9" would be interpreted as "next Thursday" (ignoring the explicit date). Now explicit month+day patterns are checked first, with day-of-week as a fallback.

### 2026-03-28
- **Scheduler**: Fixed AppleScript calendar integration failing when Calendar.app is not already running. The AppleScript now explicitly launches Calendar before attempting to create events, preventing the "Application isn't running" (-600) error.

### 2026-03-23
- **AI Scribe**: Updated system prompt to preserve clinician's language choices including profanity, slang, and colloquialisms. Previously, the model would sometimes substitute euphemisms (e.g., changing "shit" to "defecate") when proofreading notes that reflected client language. The prompt now explicitly instructs the model to respect the therapist's clinical judgment about what language to include.

---

## [1.0.0] - 2026-03-11
Initial public release.
