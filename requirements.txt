# EdgeCase Equalizer — runtime requirements (CODE_REVIEW.md L15)
#
# pyproject.toml is the source of truth for dependency *ranges*; this file
# pins the concrete versions installed in the production macOS venv.
# When adding or removing a dependency, update pyproject.toml first, then
# reflect the concrete pin here.
#
# Note on sqlcipher3: pyproject.toml declares `sqlcipher3-wheels` (prebuilt
# binary wheels for easy installs); the production macOS venv uses the
# plain `sqlcipher3` source package built against a local libsqlcipher.
# Both distributions provide the same `sqlcipher3` module.
#
# Dev/test dependencies (pytest) live in requirements-dev.txt.

blinker==1.9.0
cffi==2.0.0
charset-normalizer==3.4.4
click==8.3.1
cryptography==46.0.3
diskcache==5.6.3
Flask==3.1.2
Flask-WTF==1.2.2
iniconfig==2.3.0
itsdangerous==2.2.0
Jinja2==3.1.6
markdown==3.10
MarkupSafe==3.0.3
numpy==2.4.0
packaging==25.0
pillow==12.0.0
pluggy==1.6.0
# psutil: RAM detection for AI model sizing (guarded import in
# ai/assistant.py — the app degrades gracefully without it). Core dep in
# pyproject.toml; not pinned because it is not in the current venv yet.
psutil>=7.0.0
pycparser==2.23
Pygments==2.19.2
pypdf==6.6.2
reportlab==4.4.7
requests==2.32.5
sqlcipher3==0.5.4
typing_extensions==4.15.0
Werkzeug==3.1.5
waitress==3.0.2
pywebview==6.1

# Optional AI stack (pip install -r requirements.txt installs it; comment
# out to skip). Matches the [ai] extra in pyproject.toml.
llama_cpp_python==0.3.16
huggingface_hub==1.3.5
